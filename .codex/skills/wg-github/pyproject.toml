[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "wg-github-skill"
version = "0.1.0"
description = "Jeeves command for JSON-LD Working Group pull-request triage."
readme = "SKILL.md"
requires-python = ">=3.10"
dependencies = ["jeeves-shell"]

[tool.setuptools]
packages = []
